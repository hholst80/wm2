[project]
name = "wm2"
version = "0.1.0"
description = "A window manager for the River Wayland compositor (v0.4+)"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = [
    "pywayland>=0.4.18",
    "Pillow>=9.0",
]

[project.scripts]
wm2 = "wm2:main"
