[project]
name = "river-wm"
version = "0.1.0"
description = "A window manager for the River Wayland compositor (v0.4+)"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = [
    "pywayland>=0.4.18",
]

[project.scripts]
river-wm = "river_wm:main"
